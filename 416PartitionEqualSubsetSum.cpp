#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
int soln(vector<int> &nums, int sum)
{
    if (result )
}
bool canPartition(vector<int> &nums)
{
    int sum = 0;
    for (auto i : nums)
    {
        sum = sum + i;
    }
    soln(nums, sum);
}
int main()
{
    vector<int> arr = {1, 5, 11, 5};
    return 0;
}