#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
using namespace std;
double findMaxAverage(vector<int> &nums, int k)
{
    
}
int main()
{
    vector<int> arr = {1, 12, -5, -6, 50, 3};
    int k = 4;
    cout << findMaxAverage(arr, k);
    return 0;
}